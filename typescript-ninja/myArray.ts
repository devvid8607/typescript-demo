const superHeros:string[]=[]
const heroPower:Array<number>=[] //another way to do it

type User={
    name:string;
    email:string;
    isActive:boolean;
}

const userArray:Array<User>=[]

superHeros.push("thor")
heroPower.push(1)
userArray.push({name:"vid",email:"v",isActive:true})